# Module Hierarchy

> Generated by implementation-orchestrator from `state.json`. Do not edit manually.

- Run: `run-20260710T011551Z-369ad8`
- Workspace: `/Users/chasoik/Projects/action-tracker`
- State hash: `b3f31a9f81f193e8`
- Goal: Implement GOAL_PLAN.md P0-P8 for SAM-teacher closed-loop real-time skeleton and avatar retargeting

## Active Module Tree

- `root@r1` (ready, `no-interface@vn/a`) - Root implementation
  - `baseline-harness@r4` (accepted, `sam_goal.baseline_harness@v1`) - Build the reproducible SAM-avatar baseline harness
  - `evaluation-contract-core@r2` (accepted, `sam_goal.evaluation_contract_core@v1`) - Implement the independent evaluation contract and audit engine
  - `evaluation-contract-hardening@r1` (accepted, `sam_goal.evaluation_contract_core@v2`) - Add a source-bound v2 evaluation contract for a real manual and teacher-valid pack
  - `manual-pack-compiler@r2` (accepted, `sam_goal.manual_pack_compiler@v1`) - Build a deterministic compiler for independently authored manual video reviews
  - `manual-pack-compiler@r3` (running, `sam_goal.manual_pack_compiler_v3@v1`) - Compile blind manual reviews into an externally anchored evaluation-v3 P0 pack
  - `source-pts-manifest@r4` (accepted, `sam_goal.source_pts_manifest@v2`) - Decouple exact source identity from evaluation semantics
  - `teacher-contract-hardening@r2` (accepted, `sam_goal.teacher_contract_core@v2`) - Close teacher derivation and external phase-lock semantics

## Module Contracts

### `root@r1`

- Parent: `None`
- Status: `ready`
- Kind: `root`
- Interface: `missing@vn/a`
- Goal: Implement GOAL_PLAN.md P0-P8 for SAM-teacher closed-loop real-time skeleton and avatar retargeting

Public surface:
- None

Consumes interfaces:
- None

Owned scope:
- None

Internal scope:
- None

Acceptance criteria:
- None

Test plan:
- None

### `baseline-harness@r4`

- Parent: `root`
- Status: `accepted`
- Kind: `implementation_unit`
- Interface: `sam_goal.baseline_harness@v1`
- Goal: Provide a deterministic, resumable CLI and manifest schema that inventories all paired clips and rigs, records immutable input/runtime hashes, captures raw live recordings and legacy reports, and fails closed on partial or timed-out cells.

Public surface:
- `{"description": "Runs dry-run, selected-cell, resumed, or complete baseline capture and emits a machine-readable index.", "id": "baseline-cli", "kind": "cli", "path": "scripts/sam-goal-baseline.mjs", "symbol": "node scripts/sam-goal-baseline.mjs"}`
- `{"description": "Defines the committed baseline index and cell metadata contract.", "id": "baseline-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/baseline-schema.json"}`
- `{"description": "Documents reproducible commands, status semantics, and artifact layout.", "id": "baseline-doc", "kind": "document", "path": "docs/sam-goal-baseline.md"}`

Consumes interfaces:
- None

Owned scope:
- `scripts/sam-goal-baseline.mjs`
- `tests/sam-goal-baseline-check.mjs`
- `tests/fixtures/sam-goal-v2/baseline-schema.json`
- `docs/sam-goal-baseline.md`

Internal scope:
- `scripts/sam-goal-baseline.mjs`
- `tests/sam-goal-baseline-check.mjs`

Acceptance criteria:
- `{"criterion": "Dry-run enumerates seven paired clips, explicitly records full jujae.mp4 as unpaired, and enumerates Xbot, Soldier, and Polydancer cells.", "id": "BH-1"}`
- `{"criterion": "The emitted index records git/dirty state, source/teacher/model SHA256, runtime/browser/device/delegate/model options, exact command, raw recording/report hashes, and explicit cell status.", "id": "BH-2"}`
- `{"criterion": "Single-cell and resume modes are deterministic and never overwrite a mismatched-hash artifact as if it were the same baseline.", "id": "BH-3"}`
- `{"criterion": "Missing inputs, requested-cell timeout, partial recording, skipped required cell, and hash drift all fail closed with nonzero exit and machine-readable reason.", "id": "BH-4"}`
- `{"criterion": "Tracked changes remain within owned_scope and existing runtime/retarget scripts are unchanged.", "id": "BH-5"}`

Test plan:
- `{"kind": "functional", "maps_to": ["BH-1", "BH-2", "BH-3"], "method": "Run tests/sam-goal-baseline-check.mjs against dry-run and a fixture-backed/synthetic single-cell execution; validate output against baseline-schema."}`
- `{"kind": "edge", "maps_to": ["BH-1", "BH-3"], "method": "Verify the unpaired full jujae entry, paths with spaces, 23.976/29.97/59.94 metadata, and resume with an already completed matching cell."}`
- `{"kind": "failure", "maps_to": ["BH-4"], "method": "Inject missing asset, hash mismatch, timeout, partial report, and skipped required cell fixtures and assert nonzero status plus reason."}`
- `{"kind": "regression", "maps_to": ["BH-5"], "method": "Run npm run check and git diff --check; verify no tracked diff outside owned_scope."}`
- `{"kind": "performance", "maps_to": ["BH-1", "BH-2"], "method": "Dry-run inventory and hashing metadata setup completes without launching a browser and reports its elapsed time; document large-file hashing cost separately."}`

### `evaluation-contract-core@r2`

- Parent: `root`
- Status: `accepted`
- Kind: `implementation_unit`
- Interface: `sam_goal.evaluation_contract_core@v1`
- Goal: Provide a versioned evaluation contract, label schema, and fail-closed audit CLI that fixes clip roles, teacher-valid rule thresholds, metric denominators, coverage semantics, and stable hash behavior without reading live/student error.

Public surface:
- `{"description": "Versioned clip roles, teacher-valid rules/thresholds, metric denominators, required dimensions, and canonical hash contract.", "id": "evaluation-contract-json", "kind": "json", "path": "tests/fixtures/sam-goal-v2/evaluation-contract.json"}`
- `{"description": "Schema for clip-scoped exact-PTS/interval manual labels and required windows.", "id": "label-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/label-schema.json"}`
- `{"description": "Audits a label pack for schema, split isolation, exact denominator coverage, contact eligibility, forbidden exclusions, and stable contract hash.", "id": "label-audit-cli", "kind": "cli", "path": "scripts/sam-goal-label-audit.mjs", "symbol": "node scripts/sam-goal-label-audit.mjs"}`
- `{"description": "Documents contract fields, thresholds, denominators, audit commands, and failure semantics.", "id": "evaluation-contract-doc", "kind": "document", "path": "docs/sam-goal-evaluation-contract.md"}`

Consumes interfaces:
- None

Owned scope:
- `tests/fixtures/sam-goal-v2/evaluation-contract.json`
- `tests/fixtures/sam-goal-v2/label-schema.json`
- `tests/fixtures/sam-goal-v2/audit-fixtures/**`
- `scripts/sam-goal-label-audit.mjs`
- `tests/sam-goal-label-audit-check.mjs`
- `docs/sam-goal-evaluation-contract.md`

Internal scope:
- `tests/fixtures/sam-goal-v2/audit-fixtures/**`
- `tests/sam-goal-label-audit-check.mjs`

Acceptance criteria:
- `{"criterion": "The contract lists exactly seven paired clips with fixed roles, full jujae as unpaired, and hard_test roles for arms-crossed, csi-pose, and jujae-regression.", "id": "ECC-1"}`
- `{"criterion": "The public label schema and audit cover scenario, presence, occlusion/observability, per-foot contact, hand observability, required windows, exact PTS/intervals, unknown, and decode-unavailable semantics.", "id": "ECC-2"}`
- `{"criterion": "Metric denominator, 95 percent manual coverage, contact class support, presence macro-F1, reacquire, endpoint coverage, low-confidence, and full-strength definitions exactly match the module spec.", "id": "ECC-3"}`
- `{"criterion": "Teacher-valid generation fields have numeric thresholds and documented SAM/manual-only rationale; no live/student error field or report path is accepted.", "id": "ECC-4"}`
- `{"criterion": "Canonical contract/label hashing is deterministic across key order and fails on any rule/split/threshold/window/label mutation.", "id": "ECC-5"}`
- `{"criterion": "The audit fails closed for split leakage, missing dimensions, denominator shrinkage, insufficient contact support, malformed PTS, forbidden exclusion, and hash mismatch.", "id": "ECC-6"}`
- `{"criterion": "Tracked changes are limited to owned_scope and no live reports, runtime code, existing labels, videos, or SAM data are modified.", "id": "ECC-7"}`

Test plan:
- `{"kind": "functional", "maps_to": ["ECC-1", "ECC-2", "ECC-3", "ECC-4", "ECC-5"], "method": "Run tests/sam-goal-label-audit-check.mjs with owned synthetic valid fixtures; validate contract/schema, deterministic hash, coverage, denominators, and teacher-rule fields."}`
- `{"kind": "edge", "maps_to": ["ECC-2", "ECC-3", "ECC-5"], "method": "Test unknown, absent, occluded, out-of-frame, decode-unavailable, multi-person marker, loop epoch, fractional/non-integer PTS, and key-order permutations."}`
- `{"kind": "failure", "maps_to": ["ECC-4", "ECC-5", "ECC-6"], "method": "Use owned fixture copies to inject role leakage, missing dimension, denominator shrinkage, insufficient contact, invalid interval, live-error exclusion key, and hash mutation; assert nonzero."}`
- `{"kind": "regression", "maps_to": ["ECC-7"], "method": "Run npm run check, git diff --check, and an owned-scope diff audit."}`
- `{"kind": "performance", "maps_to": ["ECC-5", "ECC-6"], "method": "Audit a synthetic pack at or above 7,000 PTS rows without browser/model inference and report elapsed time."}`

### `evaluation-contract-hardening@r1`

- Parent: `root`
- Status: `accepted`
- Kind: `implementation_unit`
- Interface: `sam_goal.evaluation_contract_core@v2`
- Goal: Create a versioned v2 evaluation contract alongside the accepted historical contract, consuming the evaluation-independent source interface one-way and fixing the complete 6,711-row manual denominator, manual subject selection, cross-field truth rules, confidence-unavailable teacher scope semantics, phase-aware audit, and externally anchored P0-to-P1 lock chain before any student/runtime tuning.

Public surface:
- `{"description": "Source-bound v2 roles, full manual denominator, subject/review policy, teacher scope truth table, phase locks, metric definitions, and canonical identity.", "id": "evaluation-contract-json", "kind": "json", "path": "tests/fixtures/sam-goal-v2/evaluation-v2/evaluation-contract.json"}`
- `{"description": "Draft 2020-12 schema for decoder, windows with purpose tags, manual labels, subject selection, dual review/adjudication, teacher mask v2, bundles, summaries, and P0/P1 evaluation locks.", "id": "label-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/evaluation-v2/label-schema.json"}`
- `{"description": "Fail-closed phase-aware P0/P1 audit for source binding, complete denominator, labels/selection/review semantics, teacher eligibility, and external locks.", "id": "label-audit-cli", "kind": "cli", "path": "scripts/sam-goal-label-audit-v2.mjs", "symbol": "node scripts/sam-goal-label-audit-v2.mjs"}`
- `{"description": "Documents v2 migration, all denominator and truth-table semantics, confidence-unavailable rationale, lock chain, commands, and failure codes.", "id": "evaluation-contract-doc", "kind": "document", "path": "docs/sam-goal-evaluation-contract-v2.md"}`

Consumes interfaces:
- `{"interface_id": "sam_goal.evaluation_contract_core", "module_id": "evaluation-contract-core", "purpose": "Preserve all accepted split/metric/forbidden-input semantics while adding versioned anti-gaming rules without overwriting the historical surface.", "revision_id": "r2", "surface_refs": ["evaluation-contract-json", "label-schema", "label-audit-cli", "evaluation-contract-doc"]}`
- `{"interface_id": "sam_goal.source_pts_manifest", "module_id": "source-pts-manifest", "purpose": "Bind the hardened contract to the accepted exact source denominator and immutable per-clip PTS extents.", "revision_id": "r4", "surface_refs": ["source-contract", "source-schema", "decoder-manifest", "source-inventory"]}`

Owned scope:
- `tests/fixtures/sam-goal-v2/evaluation-v2/evaluation-contract.json`
- `tests/fixtures/sam-goal-v2/evaluation-v2/label-schema.json`
- `tests/fixtures/sam-goal-v2/evaluation-v2/audit-fixtures/**`
- `scripts/sam-goal-label-audit-v2.mjs`
- `tests/sam-goal-label-audit-v2-check.mjs`
- `docs/sam-goal-evaluation-contract-v2.md`

Internal scope:
- `tests/fixtures/sam-goal-v2/evaluation-v2/audit-fixtures/**`
- `tests/sam-goal-label-audit-v2-check.mjs`

Acceptance criteria:
- `{"criterion": "The contract pins source-pts r4 contract/schema/inventory plus the unchanged decoder hashes and exactly seven base full-clip windows whose union is 6,711 rows with the listed per-clip endpoints/counts; challenge overlays cannot change membership and tiny windows, any row hole, terminal omission, FPS boundaries, or unpaired leakage fail.", "id": "ECH-1"}`
- `{"criterion": "The schema and P0 audit require a separate exact-PTS manual subject-selection artifact and distinguish physical personState from SAM candidate count, including selected target identity/anchor rules.", "id": "ECH-2"}`
- `{"criterion": "Closed scenario taxonomy and presence/person/contact/hand/endpoint/subject cross-field truth rules are enforced with no ambiguous overlap or membership crossing.", "id": "ECH-3"}`
- `{"criterion": "Two complete pre-adjudication 6,711-row passes use the fixed per-clip macro Cohen-kappa/unknown/zero-variance rules; adjudication, agreement floors, 300-frame pre-mask contact support across two clips, and three exact-PTS P0 reacquire candidates across two hard-test clips are P0 hard gates, and automated proposals cannot claim review.", "id": "ECH-4"}`
- `{"criterion": "The complete teacher-mask v2 implication/reason/warning/provenance truth table preserves confidenceAvailable=false, forbids confidence imputation and r2 confidence_unavailable exclusion, permits geometry/manual fullBody/per-foot contactEligibility fallback, forces calibration false, records a warning, and still requires post-mask 100 frames per foot/class.", "id": "ECH-5"}`
- `{"criterion": "P0 and P1 audits are distinct: P0 requires no fake teacher mask and cannot claim frozen until a separate verify call supplies out-of-pack --expected-p0-lock-sha256; P1 requires that same external anchor as exact parent plus teacher-mask hash, and fully rehashed semantic/parent-lock mutations fail.", "id": "ECH-6"}`
- `{"criterion": "Adversarial focused tests with exactly 6,711 decoder rows and at least 7,000 total schema artifact rows, npm run check, git diff --check, and owned-scope audit pass without reading live/student output or modifying actual labels/source artifacts or the accepted historical v1 contract.", "id": "ECH-7"}`

Test plan:
- `{"kind": "functional", "maps_to": ["ECH-1", "ECH-2", "ECH-3", "ECH-4", "ECH-6"], "method": "Build a synthetic full seven-clip P0 pack with exactly 6,711 decoder rows plus enough label/review rows for at least 7,000 total schema artifacts; require two complete pre-adjudication passes, resolved adjudication, contact/reacquire support, policy/summary, and a clean P0 audit without a teacher mask, then separately verify its candidate lock with an out-of-pack expected hash argument."}`
- `{"kind": "functional", "maps_to": ["ECH-5", "ECH-6"], "method": "Extend the externally anchored P0 pack to P1 with confidence-unavailable but finite/manual-observable teacher rows and a child lock; verify fullBody and per-foot contactEligibility true, calibration false, exact provenance/warning presence, and post-mask support."}`
- `{"kind": "edge", "maps_to": ["ECH-1", "ECH-2", "ECH-3", "ECH-5"], "method": "Cover nonzero initial PTS, terminal last+1 boundary, loop epoch, unknown/absent/out-of-frame, multiple physical people with anchored target, one physical person with duplicate SAM candidates, confidence present vs unavailable, and interval compiler splits at overlay memberships."}`
- `{"kind": "failure", "maps_to": ["ECH-1", "ECH-2", "ECH-3", "ECH-4"], "method": "Fail tiny/base-window substitution, any of 6,711 row holes including deleted final frame, FPS-like boundary, open scenario tag, missing/ambiguous subject, label-selection gap/overlap, every cross-field contradiction, same reviewer, adjudicated-pass substitution, unresolved disagreement, automated reviewed status, low deterministic kappa, low contact support, and too few/hard-test-poor P0 reacquire candidates."}`
- `{"kind": "failure", "maps_to": ["ECH-5", "ECH-6"], "method": "Fail detector-score-as-joint-confidence, imputed confidence, confidence-unavailable calibration true, each invalid valid/scope/per-foot-contactEligibility/reason/warning/provenance combination, post-mask support shrink, missing/wrong external P0 anchor or in-pack parent, student/live field injection, and fully recomputed self-hashes after semantic mutation."}`
- `{"kind": "contract", "maps_to": ["ECH-1", "ECH-6", "ECH-7"], "method": "Verify exact source-pts r4 contract/schema/inventory and unchanged decoder hashes/counts, Draft 2020-12 validation for every generated valid/invalid artifact, stable root-only self-hash exclusion under object-key reorder, ordered-array semantics, no wall-clock keys in locked artifacts, and independent external lock anchors."}`
- `{"kind": "regression", "maps_to": ["ECH-7"], "method": "Run the focused suite, npm run check, git diff --check, and strict owned/read/forbidden scope audit; preserve user untracked assets, accepted historical v1 contract surfaces, and source-pts r4 files byte-for-byte."}`
- `{"kind": "performance", "maps_to": ["ECH-7"], "method": "Audit a valid exact 6,711-decoder-row pack with at least 7,000 total schema artifact rows plus adversarial variants without browser/model inference and report elapsed time."}`

### `manual-pack-compiler@r2`

- Parent: `root`
- Status: `accepted`
- Kind: `implementation_unit`
- Interface: `sam_goal.manual_pack_compiler@v1`
- Goal: Implement a fail-closed, deterministic compiler that validates two independently authored interval reviews and explicit adjudication decisions, maps them only onto the accepted 6,711 exact decoder identities, emits the evaluation-v2 P0 artifact set atomically, and never infers or fabricates a manual decision.

Public surface:
- `{"description": "Closed visual-review and adjudication authoring contract.", "id": "authoring-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/evaluation-v2/authoring-schema.json"}`
- `{"description": "Closed out-of-pack write-once external P0 anchor contract.", "id": "p0-lock-anchor-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/evaluation-v2/p0-lock-anchor-schema.json"}`
- `{"description": "Read only one review and materialize/report it without opening the other review or final pack.", "id": "manual-pack-review-validator-cli", "kind": "cli", "path": "scripts/sam-goal-manual-pack-v2.mjs", "symbol": "node scripts/sam-goal-manual-pack-v2.mjs validate-review --review <path> --expected-role <first|second>"}`
- `{"description": "Compile sealed inputs to a new, previously nonexistent output directory; never decides labels.", "id": "manual-pack-compiler-cli", "kind": "cli", "path": "scripts/sam-goal-manual-pack-v2.mjs", "symbol": "node scripts/sam-goal-manual-pack-v2.mjs compile --review-a <path> --review-b <path> --adjudication <path> --output-dir <new-path>"}`
- `{"description": "Write-once external-anchor creation and separate-process verification modes; both bind/recompute the sealed raw authoring inputs explicitly.", "id": "p0-anchor-cli", "kind": "cli", "path": "scripts/sam-goal-manual-pack-v2.mjs", "symbol": "node scripts/sam-goal-manual-pack-v2.mjs <create-anchor|verify-anchor> --anchor <path> --label-dir <path> --review-a <path> --review-b <path> --adjudication <path>"}`

Consumes interfaces:
- `{"interface_id": "sam_goal.evaluation_contract_core", "module_id": "evaluation-contract-hardening", "purpose": "Emit only artifacts accepted by the frozen v2 manual/P0 contract.", "revision_id": "r1", "surface_refs": ["evaluation-contract-json", "label-schema", "label-audit-cli", "evaluation-contract-doc"]}`
- `{"interface_id": "sam_goal.source_pts_manifest", "module_id": "source-pts-manifest", "purpose": "Map authoring intervals to the immutable exact source identities.", "revision_id": "r4", "surface_refs": ["decoder-manifest", "source-inventory"]}`

Owned scope:
- `tests/fixtures/sam-goal-v2/evaluation-v2/authoring-schema.json`
- `tests/fixtures/sam-goal-v2/evaluation-v2/p0-lock-anchor-schema.json`
- `tests/fixtures/sam-goal-v2/evaluation-v2/compiler-fixtures/**`
- `scripts/sam-goal-manual-pack-v2.mjs`
- `tests/sam-goal-manual-pack-v2-check.mjs`
- `docs/sam-goal-manual-pack-compiler-v2.md`

Internal scope:
- `tests/fixtures/sam-goal-v2/evaluation-v2/compiler-fixtures/**`

Acceptance criteria:
- `{"criterion": "Draft 2020-12 authoring and external-anchor schemas close reviewerPseudonymSha256 separately from root expectedCanonicalHash, source hashes, seven-clip interval coverage, exact frame-index boundaries, complete label plus subject state, window overlays, explicit adjudication decisions, and write-once external lock fields; unknown keys, wall clocks, automated-review claims, and forbidden input families fail.", "id": "MPC-1"}`
- `{"criterion": "The compiler maps frame-index intervals through the accepted order-sensitive decoder manifest, including nonzero initial PTS, rejects every gap/overlap/out-of-range/boundary/membership crossing, and independently materializes exactly 6,711 review rows per pass without consulting the other pass.", "id": "MPC-2"}`
- `{"criterion": "All raw field differences including subject state/target/anchor require an explicit adjudication decision; agreed or adjudicated state alone produces final compact label and subject intervals, and intervals split at every window-membership boundary.", "id": "MPC-3"}`
- `{"criterion": "Output is deterministic and atomic: identical sealed inputs produce byte-identical JSON/JSONL and P0 candidate manifest hashes, failed compilation leaves no partial/stale files, and no wall-clock value enters a durable artifact.", "id": "MPC-4"}`
- `{"criterion": "Focused tests exercise exact 6,711-row full coverage, subject disagreements, contact/reacquire support gates through the accepted auditor, input-order/hash drift, stale/partial output, and forbidden SAM/live/student leakage; syntax, npm run check, git diff --check, Draft schema, scope and temp cleanup pass.", "id": "MPC-5"}`

Test plan:
- `{"kind": "functional", "maps_to": ["MPC-1", "MPC-2", "MPC-3", "MPC-4"], "method": "Compile two independent synthetic seven-clip authoring packs plus adjudication into a valid candidate, audit all 6,711 identities, then repeat and require byte-identical artifacts and lock."}`
- `{"kind": "edge", "maps_to": ["MPC-1", "MPC-2", "MPC-3"], "method": "Cover nonzero initial PTS, terminal last+1, a label split at every overlay membership boundary, absent/unknown/partial body, anchored multiple people, subject target/anchor disagreement, and zero-disagreement final binding."}`
- `{"kind": "failure", "maps_to": ["MPC-1", "MPC-2", "MPC-3", "MPC-5"], "method": "Fail the same review path supplied to both roles, duplicate reviewerPseudonymSha256, role mismatch, sealed expectedCanonicalHash drift, missing interval, overlap, invalid frame boundary, open field, unreviewed subject transition, wrong disagreement path, final-only label injection, automated origin, forbidden output field, low compiled support, too few compiled reacquire events, and any pinned hash drift."}`
- `{"kind": "regression", "maps_to": ["MPC-4", "MPC-5"], "method": "Run focused test, accepted v2 label auditor, npm run check, syntax, Draft 2020-12 validation, git diff --check, owned-scope audit, and verify zero temp residue."}`

### `manual-pack-compiler@r3`

- Parent: `root`
- Status: `running`
- Kind: `implementation_unit`
- Interface: `sam_goal.manual_pack_compiler_v3@v1`
- Goal: Implement the evaluation-v3 manual review compiler only after the teacher contract is accepted: validate each blind review in isolation, transform two sealed reviews plus explicit field-level adjudication without inferring labels, emit the closed 6,711-row P0 candidate atomically, and create or independently verify the write-once external P0-v2 anchor that binds every v3 rule, source, manual and tool byte.

Public surface:
- `{"description": "Deep-equal and declared-serialization-byte-equal implementation of spec.authoring_schema_contract.document: the closed evaluation-v3 root oneOf contract with public `$defs.review` and `$defs.adjudication`, exact source/hash bindings, current teacher reviewState vocabulary, and closed typed disagreement/decision values consumed identically by compiler and auditor.", "id": "authoring-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/evaluation-v3/authoring-schema.json"}`
- `{"description": "Read-only one-pass validation that cannot open the counterpart review or any final pack.", "id": "manual-pack-review-validator-cli", "kind": "cli", "path": "scripts/sam-goal-manual-pack-v3.mjs", "symbol": "node scripts/sam-goal-manual-pack-v3.mjs validate-review --review <path> --expected-role <first|second> --expected-reviewer-pseudonym-sha256 <sha256>"}`
- `{"description": "Deterministically compile sealed manual facts into an atomically committed v3 P0 candidate without deciding labels.", "id": "manual-pack-compiler-cli", "kind": "cli", "path": "scripts/sam-goal-manual-pack-v3.mjs", "symbol": "node scripts/sam-goal-manual-pack-v3.mjs compile --review-a <path> --review-b <path> --adjudication <path> --output-dir <absent-path>"}`
- `{"description": "Write-once external P0-v2 anchor creation returning its canonical hash.", "id": "p0-anchor-create-cli", "kind": "cli", "path": "scripts/sam-goal-manual-pack-v3.mjs", "symbol": "node scripts/sam-goal-manual-pack-v3.mjs create-anchor --anchor <absent-external-path> --label-dir <candidate> --review-a <path> --review-b <path> --adjudication <path>"}`
- `{"description": "Read-only independent expected-hash verification of the full external P0 chain.", "id": "p0-anchor-verify-cli", "kind": "cli", "path": "scripts/sam-goal-manual-pack-v3.mjs", "symbol": "node scripts/sam-goal-manual-pack-v3.mjs verify-anchor --anchor <external-path> --expected-p0-anchor-sha256 <sha256> --label-dir <candidate> --review-a <path> --review-b <path> --adjudication <path>"}`
- `{"description": "Normative blindness, transformation, deterministic serialization, atomic commit, anchor and failure semantics.", "id": "manual-pack-compiler-doc", "kind": "markdown", "path": "docs/sam-goal-manual-pack-compiler-v3.md"}`

Consumes interfaces:
- `{"interface_id": "sam_goal.teacher_contract_core", "module_id": "teacher-contract-hardening", "purpose": "Consume the authoritative evaluation-v3 authoring, pack, hash-DAG, audit and external-anchor contract without changing any policy or threshold.", "revision_id": "r2", "surface_refs": ["evaluation-contract-json", "label-schema", "teacher-input-inventory", "teacher-policy", "teacher-schema", "p0-lock-anchor-v2-schema", "p1-lock-anchor-schema", "label-audit-p0-candidate-cli", "label-audit-p0-cli", "evaluation-contract-doc"]}`
- `{"interface_id": "sam_goal.source_pts_manifest", "module_id": "source-pts-manifest", "purpose": "Map manual intervals only onto immutable accepted source and exact rational decoder identities.", "revision_id": "r4", "surface_refs": ["source-contract", "source-schema", "source-inventory", "decoder-manifest"]}`

Owned scope:
- `tests/fixtures/sam-goal-v2/evaluation-v3/authoring-schema.json`
- `tests/fixtures/sam-goal-v2/evaluation-v3/manual-compiler-fixtures/**`
- `scripts/sam-goal-manual-pack-v3.mjs`
- `tests/sam-goal-manual-pack-v3-check.mjs`
- `docs/sam-goal-manual-pack-compiler-v3.md`

Internal scope:
- `tests/fixtures/sam-goal-v2/evaluation-v3/manual-compiler-fixtures/**`

Acceptance criteria:
- `{"criterion": "The revision is exactly manual-pack-compiler@r3 with supersedes=r2, satisfying the teacher-contract-hardening@r2 literal migration gate, while declaring no dependency or consumed interface on inactive manual-pack-compiler@r2. The three accepted r2 historical byte pins in spec.historical_r2_regression_inputs remain exact and those files remain immutable; r2 transactional behavior is regression evidence only, never live interface authority. A closed Draft 2020-12 evaluation-v3 authoring schema is deep-equal to spec.authoring_schema_contract.document, byte-equal to its declared serialization, and represents exactly one artifact through public root oneOf branches referencing `$defs.review` or `$defs.adjudication`. `$defs.review` requires artifactType=sam-goal-manual-review-v3, schemaVersion=3 and role exactly first|second; `$defs.adjudication` requires artifactType=sam-goal-manual-adjudication-v3, schemaVersion=3 and role exactly adjudication. Reviews bind every named accepted v3 evaluation/label/authoring/teacher/anchor/source/decoder hash and exactly seven source descriptors; use the exact current handObservability and endpointObservability vocabulary; cover all 6,711 decoder identities with nonoverlapping sourceFrameIndex intervals and no automated truth. Adjudication binds A/B canonical hash, byte hash and pseudonym and uses only the closed typed disagreement registry. Reviewer/adjudicator pseudonyms are distinct role identities, never mutable file hashes. The v3 auditor reads these same public definitions/constants from the P0-bound authoring schema; no synthetic/v2 alternate type vocabulary is accepted.", "id": "MPC3-1"}`
- `{"criterion": "validate-review reads one immutable review Buffer and only accepted public contract/source inputs, never the counterpart review, adjudication, label directory or final pack. It checks exact role and independently supplied reviewer pseudonym, materializes exactly 6,711 exact decoder identities, enforces complete windows, truth-table, reacquire, per-side contact and head/hand support gates, reports proposal evidence without writing, and fails unknown keys, wall-clock coordinates, policy drift or forbidden SAM/live/student/avatar families.", "id": "MPC3-2"}`
- `{"criterion": "compile accepts two byte-distinct, role-distinct, pseudonym-distinct sealed reviews and one distinct sealed adjudication. Every disagreement in scenarios or any manual/subject/window field is addressed by the closed union-boundary segment and keyed-window RFC 6901 grammar, sorted UTF-8 bytewise, and receives exactly one full typed final value; parent/child duplicates and implementation-selected array/index paths are impossible. Agreement or adjudication is the only source of output and the compiler never decides. It emits exactly the nine closed P0 candidate files, preserves all 6,711 identities in each applicable JSONL, retains scenario evidence in both review and adjudication rows, invokes v3 candidate audit, and contains no P1/live/student/avatar field.", "id": "MPC3-3"}`
- `{"criterion": "The P0 candidate descriptor DAG matches teacher-contract-hardening@r2 exactly: evaluation-pack.json binds evaluation contract, label and v3 authoring schemas, teacher input inventory/policy/schema, P0-v2/P1 anchor schemas, source inventory, decoder manifest, all manual artifacts, compiler bytes and v3 auditor bytes. Candidate self-hash is candidateP0PackCanonicalSha256 only and cannot claim frozen/verified; duplicate, missing, extra, renamed, traversal or rehashed descriptors fail.", "id": "MPC3-4"}`
- `{"criterion": "All JSON/JSONL/tool hashes and semantics come from one immutable Buffer snapshot per path. Compilation uses registered sibling staging, deterministic fsynced writes and the accepted macOS atomic no-replace commit with no ordinary-rename fallback; destination races preserve the competitor inode/bytes. Success, semantic failure, child failure, SIGINT and SIGTERM leave no sibling temp or partial destination and never replace/merge an existing destination.", "id": "MPC3-5"}`
- `{"criterion": "create-anchor writes once at an absent nonsymlink regular-file path outside the candidate, with link count one and a device/inode distinct from every pack artifact and all three sealed inputs, and binds the exact candidate, nine compiled bytes, complete dependency hashes and fixed logical role descriptors for review A, review B and adjudication under p0-lock-anchor-v2-schema; actual caller paths never enter durable bytes. verify-anchor is read-only, requires an independently supplied expected P0 anchor canonical hash, maps the three actual CLI paths to the fixed roles, single-snapshot/no-follow verifies every external file and ancestor identity, recompiles and byte-compares the pack, invokes anchored v3 P0 audit with the same three paths, and rejects in-pack, role-swapped, byte/inode-aliased, symlink, hardlink, substituted, mutated, TOCTOU-swapped or self-selected anchors.", "id": "MPC3-6"}`
- `{"criterion": "Focused attacks fail for v2 pin reuse, contract/policy/teacher-input/schema/tool substitution, scenario-only disagreement loss, missing or extra adjudication, role/pseudonym/hash/realpath/device+inode aliasing across all three sealed inputs, logicalPath filesystem resolution, decoder/order/count drift, forbidden leakage, descriptor set/path attacks, candidate hash used as external parent, free caller anchor substitution, single-read mutation, final-component or ancestor replacement, stale destination and coordinated post-precheck races. Synthetic exact-6,711 success, arbitrary outside-repository input/anchor paths, Draft 2020-12 validation, deterministic repeated output, existing tests, npm run check, syntax, diff, strict scope/isolation and temp cleanup all pass before any real P0 operation is allowed.", "id": "MPC3-7"}`

Test plan:
- `{"kind": "functional", "maps_to": ["MPC3-1", "MPC3-2", "MPC3-3", "MPC3-4", "MPC3-6"], "method": "Assert module_id=manual-pack-compiler, revision_id=r3 and supersedes=r2; assert dependencies and consumes_interfaces contain no manual-pack-compiler@r2 edge. Before any v3 test, byte-hash the three read-only files in spec.historical_r2_regression_inputs and require the declared pins; repeat after the suite and require unchanged bytes. Materialize spec.authoring_schema_contract.document deep-equal and byte-equal under its declared serialization as authoring-schema.json and independently Draft202012Validator.check_schema it. Generate two synthetic but complete and independently sealed sam-goal-manual-review-v3/schemaVersion=3 artifacts with roles first and second over the accepted 6,711-row decoder denominator plus one sam-goal-manual-adjudication-v3/schemaVersion=3/role=adjudication artifact containing every binding/reference field and each valueType branch. Validate all three against the exact public `$defs.review`/`$defs.adjudication` branches also consumed by the v3 auditor; validate each review in a process whose read instrumentation forbids the counterpart; compile twice to distinct absent paths; require byte-identical nine-file candidates and exact compiled_mapping rows/PTS/fields/descriptors; create an external anchor and verify it in a separate process using a separately supplied expected canonical hash."}`
- `{"kind": "edge", "maps_to": ["MPC3-1", "MPC3-2", "MPC3-3", "MPC3-5", "MPC3-6"], "method": "Exercise nonzero initial PTS, terminal last-PTS-plus-one, final frame/window, adjacent interval merge and window-membership boundaries, normalized anchor edges, absent/unknown/reacquire truth states, independent left/right exact-PTS contact confirmation, every handObservability and endpointObservability leaf, scenario add/remove, anchor null-to-object/object-to-null/x-only/y-only compiled dotted-field changes, keyed window add/delete/reorder/start/end/purposeTags/scenarioTags changes, discontinuous segments requiring different final values for the same leaf, zero disagreements, every manual field disagreement, symlink/hardlink/inside-pack anchor cases, existing destination, and all equality support thresholds."}`
- `{"kind": "failure", "maps_to": ["MPC3-2", "MPC3-3", "MPC3-4", "MPC3-5", "MPC3-6", "MPC3-7"], "method": "Run every MPC3-7 semantic and filesystem attack, recomputing attacker-controlled self-hashes where applicable. Include missing/extra sourceBinding pins, wrong seven-source projection/order, A/B canonical/byte/pseudonym reference drift, unknown or path-incompatible valueType, decision/disagreement type mismatch, generic observability or palm/finger alias, disagreement parent/child duplication, illegal array-index addressing, RFC 6901 escaping drift, UTF-8 path-order drift, window raw-order masquerading as a semantic difference, merged discontinuous same-leaf decisions, real child SIGINT=130 and SIGTERM=143 during compile/auditor/anchor helpers, a post-precheck destination competitor with inode/marker preservation, PATH-shadowed rename helper, read-after-hash mutation, external expected-hash substitution, descriptor traversal/duplicate/extra/missing and complete v2-pin replay. Require nonzero with destination/anchor/inputs unchanged and zero registered residue."}`
- `{"kind": "regression", "maps_to": ["MPC3-5", "MPC3-7"], "method": "Treat the pinned accepted r2 authoring schema, P0 schema and compiler bytes only as immutable read-only regression inputs, never as a dependency or consumed interface. Run the accepted compiler-r2 focused suite unchanged and require its blind validation, atomic absent-destination compile, macOS no-replace race preservation, write-once external anchor, read-only verification, signal cleanup and zero-residue behavior to remain unchanged; then rerun all three historical byte pins. Run the new compiler-v3 suite and v3 label-auditor candidate/anchored modes on the exact same realistic v3 review/adjudication fixtures. Prove valid public `$defs.review`/`$defs.adjudication` instances pass both compiler and auditor, while every v2 artifactType/schemaVersion, missing/wrong adjudication role and private synthetic alternate fails. Validate all Draft 2020-12 schemas, node --check, npm run check, git diff --check, orchestrator strict isolation and explicit owned/read/forbidden path audit."}`
- `{"kind": "performance", "maps_to": ["MPC3-2", "MPC3-3", "MPC3-5", "MPC3-7"], "method": "On the complete synthetic 6,711-identity A/B/adjudication fixture, report validate-review, compile, create-anchor and verify-anchor wall time and peak RSS separately; assert input snapshots and emitted JSONL processing remain bounded by the declared artifact sizes rather than unbounded duplicate row graphs, and record stage/auditor/atomic-commit timing without weakening any semantic or filesystem check."}`

### `source-pts-manifest@r4`

- Parent: `root`
- Status: `accepted`
- Kind: `implementation_unit`
- Interface: `sam_goal.source_pts_manifest@v2`
- Goal: Replace source-pts r2's evaluation-contract-bound inventory with a standalone, source-only contract and Draft 2020-12 schema while preserving the accepted 6,711 decoder rows byte-for-byte, all source/media identities, fail-closed drift checks, TOCTOU barriers, and transactional commit behavior.

Public surface:
- `{"description": "Standalone exact source-set membership and schema identity with no evaluation semantics.", "id": "source-contract", "kind": "json", "path": "tests/fixtures/sam-goal-v2/source-contract.json"}`
- `{"description": "Draft 2020-12 source-contract, decoder-row, and source-inventory schemas.", "id": "source-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/source-schema.json"}`
- `{"description": "Generates/checks exact decoder PTS and source inventory using only the standalone source contract/schema and local media.", "id": "source-pts-cli", "kind": "cli", "path": "scripts/sam-goal-source-pts.mjs", "symbol": "node scripts/sam-goal-source-pts.mjs"}`
- `{"description": "Evaluation-independent exact ordered PTS denominator; byte-identical to accepted r2.", "id": "decoder-manifest", "kind": "jsonl", "path": "tests/fixtures/sam-goal-v2/labels/decoder-manifest.jsonl"}`
- `{"description": "Evaluation-independent source/media/ffprobe/decoder identity bound only to source contract/schema.", "id": "source-inventory", "kind": "json", "path": "tests/fixtures/sam-goal-v2/labels/source-inventory.json"}`
- `{"description": "Migration, independence, exact PTS, transaction, generation/check, and drift semantics.", "id": "source-pts-doc", "kind": "document", "path": "docs/sam-goal-source-pts.md"}`

Consumes interfaces:
- None

Owned scope:
- `tests/fixtures/sam-goal-v2/source-contract.json`
- `tests/fixtures/sam-goal-v2/source-schema.json`
- `scripts/sam-goal-source-pts.mjs`
- `tests/sam-goal-source-pts-check.mjs`
- `tests/fixtures/sam-goal-v2/labels/decoder-manifest.jsonl`
- `tests/fixtures/sam-goal-v2/labels/source-inventory.json`
- `docs/sam-goal-source-pts.md`

Internal scope:
- `tests/sam-goal-source-pts-check.mjs`

Acceptance criteria:
- `{"criterion": "Source contract/schema/inventory/CLI contain no evaluation contract, label schema, role/split/teacher/manual/live/student/avatar dependency; a source-only isolated fixture run succeeds.", "id": "SIC-1"}`
- `{"criterion": "The migrated real decoder manifest remains exactly 6,711 rows with byte SHA d300ac13... and ordered canonical SHA dde8d9e0..., including identical per-clip row bytes/order/PTS; full jujae remains 2,189 probed and 0 paired.", "id": "SIC-2"}`
- `{"criterion": "Standalone Draft 2020-12 source schema validates the accepted source contract, all real decoder rows, and source inventory; internally rehashed semantic contract/schema variants are rejected by hard-pinned hashes before ffprobe.", "id": "SIC-3"}`
- `{"criterion": "All r2 fail-closed guarantees remain: exact argv/rational/BigInt PTS, wrong stream/time-base and malformed PTS rejection, physical/semantic/order/count/source/media drift dimensions, source-set TOCTOU barriers, read-only check, and pair rollback.", "id": "SIC-4"}`
- `{"criterion": "Fresh real generation/check passes with drift 0; the new source inventory identity is deterministic and changes only because obsolete evaluation bindings were removed and standalone source bindings added.", "id": "SIC-5"}`
- `{"criterion": "Focused adversarial suite, real schema validation, npm run check, git diff --check, and owned-scope audit pass while user untracked files and all evaluation/manual/teacher/runtime files remain untouched.", "id": "SIC-6"}`

Test plan:
- `{"kind": "functional", "maps_to": ["SIC-1", "SIC-2", "SIC-3", "SIC-5"], "method": "Run the CLI with standalone source contract/schema, seven controlled paired videos, full unpaired video, and exact fake ffprobe; validate source-only inventory, schemas, hashes, isolated inputs, repeated determinism, and check mode."}`
- `{"kind": "migration", "maps_to": ["SIC-1", "SIC-2", "SIC-5"], "method": "Snapshot accepted r2 decoder bytes/hash/rows before changes; after migration assert byte-for-byte equality, exact old physical/canonical hashes, same 6,711/2,189 counts, absence of old evaluation fields, and deterministic new inventory hash."}`
- `{"kind": "edge", "maps_to": ["SIC-2", "SIC-4"], "method": "Retain paths with spaces/metacharacters, absolute stream index 1, nonzero initial PTS, 43080/1801 and 60000/1001 rates, ticks above Number.MAX_SAFE_INTEGER, extra ffprobe fields, terminal LF, and source mutation between earlier/later probes."}`
- `{"kind": "failure", "maps_to": ["SIC-1", "SIC-3", "SIC-4"], "method": "Fail source contract semantic mutation with fully recomputed self-hash, changed schema with both hashes recomputed, forbidden role/teacher/evaluation fields, unknown/duplicate/unpaired override, wrong stream/time base, missing/fractional/exponent/negative/duplicate/nonmonotonic PTS, source mutation, row deletion/reorder, physical-only drift, malformed JSON, and second commit fault."}`
- `{"kind": "contract", "maps_to": ["SIC-1", "SIC-2", "SIC-3", "SIC-5"], "method": "Validate every real artifact with Draft 2020-12, compare source contract/schema declared and hard-pinned hashes, prove decoder arrays are order-sensitive, prove unrelated evaluation fixture mutation has no output effect, and run a static forbidden-dependency surface scan."}`
- `{"kind": "regression", "maps_to": ["SIC-6"], "method": "Run node focused/syntax checks, fresh real --check, npm run check, git diff --check, and strict owned/read/forbidden scope audit."}`
- `{"kind": "performance", "maps_to": ["SIC-5"], "method": "Report real source-only generation/check elapsed time, rows, and peak RSS without browser/model/SAM inference."}`

### `teacher-contract-hardening@r2`

- Parent: `root`
- Status: `accepted`
- Kind: `implementation_unit`
- Interface: `sam_goal.teacher_contract_core@v2`
- Goal: Publish an isolated evaluation-v3 contract that supersedes the accepted historical v2 evaluation contract before real P0 generation so teacher subject selection, raw-data preservation, geometry and temporal validity are fully declarative and numerically frozen in P0; make audits recompute rather than trust claimed mask booleans; and bind P1 to the write-once external P0 anchor rather than a mutable in-pack candidate hash.

Public surface:
- `{"description": "Frozen manual, split, teacher derivation and phase-lock policy.", "id": "evaluation-contract-json", "kind": "json", "path": "tests/fixtures/sam-goal-v2/evaluation-v3/evaluation-contract.json"}`
- `{"description": "Closed manual/P0 and pack descriptor schema.", "id": "label-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/evaluation-v3/label-schema.json"}`
- `{"description": "Self-hashed byte inventory of the seven raw teacher JSONL/metadata/summary inputs, frozen in P0 before validity materialization.", "id": "teacher-input-inventory", "kind": "json", "path": "tests/fixtures/sam-goal-v2/evaluation-v3/teacher-input-inventory.json"}`
- `{"description": "Self-hashed numeric subject/geometry/temporal derivation policy frozen in P0.", "id": "teacher-policy", "kind": "json", "path": "tests/fixtures/sam-goal-v2/evaluation-v3/teacher-policy.json"}`
- `{"description": "Lossless Dataset v2, deterministic refined teacher, mask, source/summary and P1 lock contract.", "id": "teacher-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/evaluation-v3/teacher-schema.json"}`
- `{"description": "Standalone external P0 anchor v2 schema consumed by mandatory downstream compiler r3.", "id": "p0-lock-anchor-v2-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/evaluation-v3/p0-lock-anchor-v2-schema.json"}`
- `{"description": "Standalone write-once external P1 anchor schema consumed by the later P1 materializer.", "id": "p1-lock-anchor-schema", "kind": "json-schema", "path": "tests/fixtures/sam-goal-v2/evaluation-v3/p1-lock-anchor-schema.json"}`
- `{"description": "Candidate-only audit; rejects every anchor argument and can never report verified or frozen.", "id": "label-audit-p0-candidate-cli", "kind": "cli", "path": "scripts/sam-goal-label-audit-v3.mjs", "symbol": "node scripts/sam-goal-label-audit-v3.mjs --label-dir <dir> --phase p0-candidate"}`
- `{"description": "Requires a concrete P0 anchor, independent expected external anchor hash and the three role-bound sealed inputs; verifies each exact single snapshot against fixed logical descriptors and never persists caller paths.", "id": "label-audit-p0-cli", "kind": "cli", "path": "scripts/sam-goal-label-audit-v3.mjs", "symbol": "node scripts/sam-goal-label-audit-v3.mjs --label-dir <dir> --phase p0 --p0-anchor <path> --expected-p0-anchor-sha256 <sha256> --review-a <path> --review-b <path> --adjudication <path>"}`
- `{"description": "Requires and verifies the complete external P0/P1 anchor chain and recomputes teacher facts; the independently expected P0 anchor is authoritative, so raw sealed inputs are deliberately not reopened or required in P1.", "id": "label-audit-p1-cli", "kind": "cli", "path": "scripts/sam-goal-label-audit-v3.mjs", "symbol": "node scripts/sam-goal-label-audit-v3.mjs --label-dir <dir> --phase p1 --p0-anchor <path> --expected-p0-anchor-sha256 <sha256> --p1-anchor <path> --expected-p1-anchor-sha256 <sha256>"}`
- `{"description": "Normative algorithms, units, reason precedence and phase lock commands.", "id": "evaluation-contract-doc", "kind": "markdown", "path": "docs/sam-goal-evaluation-contract-v3.md"}`

Consumes interfaces:
- `{"interface_id": "sam_goal.evaluation_contract_core", "module_id": "evaluation-contract-hardening", "purpose": "Supersede the accepted manual/P0 base without weakening any existing manual, support or leakage guard.", "revision_id": "r1", "surface_refs": ["evaluation-contract-json", "label-schema", "label-audit-cli", "evaluation-contract-doc"]}`
- `{"interface_id": "sam_goal.source_pts_manifest", "module_id": "source-pts-manifest", "purpose": "Bind lossless teacher rows to the immutable exact decoder identities.", "revision_id": "r4", "surface_refs": ["source-contract", "source-schema", "source-inventory", "decoder-manifest"]}`
- `{"interface_id": "sam_goal.manual_pack_compiler", "module_id": "manual-pack-compiler", "purpose": "Consume and strengthen the concrete write-once P0 anchor schema and candidate compiler interface.", "revision_id": "r2", "surface_refs": ["authoring-schema", "p0-lock-anchor-schema", "manual-pack-review-validator-cli", "manual-pack-compiler-cli", "p0-anchor-cli"]}`

Owned scope:
- `tests/fixtures/sam-goal-v2/evaluation-v3/evaluation-contract.json`
- `tests/fixtures/sam-goal-v2/evaluation-v3/label-schema.json`
- `tests/fixtures/sam-goal-v2/evaluation-v3/teacher-input-inventory.json`
- `tests/fixtures/sam-goal-v2/evaluation-v3/teacher-policy.json`
- `tests/fixtures/sam-goal-v2/evaluation-v3/teacher-schema.json`
- `tests/fixtures/sam-goal-v2/evaluation-v3/p0-lock-anchor-v2-schema.json`
- `tests/fixtures/sam-goal-v2/evaluation-v3/p1-lock-anchor-schema.json`
- `tests/fixtures/sam-goal-v2/evaluation-v3/audit-fixtures/**`
- `scripts/sam-goal-label-audit-v3.mjs`
- `tests/sam-goal-label-audit-v3-check.mjs`
- `docs/sam-goal-evaluation-contract-v3.md`

Internal scope:
- `tests/fixtures/sam-goal-v2/evaluation-v3/audit-fixtures/**`

Acceptance criteria:
- `{"criterion": "A separately self-hashed teacher-input inventory, teacher-policy artifact and standalone teacher schema are bound one-way by the evaluation contract and P0 descriptors without referring back to the evaluation contract hash. Before real reviews compile, the inventory freezes byte and canonical hashes for all seven clips' skeletons_mhr70.jsonl, metadata_mhr70.json and summary.json plus their accepted source-video/decoder identities. Policy and schema fix exact decoder-line alignment, MHR70 index/name semantics, raw timestamp retention, all-person preservation, raw person ID versus persistent manual target ID, summary-reconciled detector hit/carry-forward-fallback/miss provenance, native-confidence-unavailable semantics, candidate selection, coordinate spaces/units, reprojection, torso basis, in-frame, bone/scale/speed/gap guards, scope derivation, deterministic temporal refinement with raw-hard/refined-watch roles, reason precedence, and split roles; no implementation-defined threshold or hash cycle remains.", "id": "ECH2-1"}`
- `{"criterion": "Candidate selection is deterministic and manual-first with one closed branch order: zero candidates is missing; a single candidate with null anchor is selected without deriving an effective bbox, while a non-null anchor requires a usable expanded effective bbox; multiple candidates with null anchor are ambiguous before any bbox inspection, while non-null anchor checks bbox usability before requiring exactly one containment. Detector count/score never sets physical personState or joint confidence; selectedTrackId is the stable manualTargetId while raw person_id/index remains intact.", "id": "ECH2-2"}`
- `{"criterion": "Teacher schema closes byte-lossless 6,711-row Dataset v2 plus independently reproducible 6,711-row refined-teacher/mask contracts against the P0-frozen teacher-input inventory: all seven current raw JSONL files are explicitly recorded as UTF-8 CRLF with terminal CRLF; each Dataset row embeds the exact original raw JSON object bytes excluding the complete CRLF terminator as base64 plus line byte/canonical hashes, so every frame/person field (MHR70 2D/3D, MHR127, both bboxes, detector score, focal length, camera translation, global rotation, person ID), key/number spelling, null/missing state and person order reconstruct exactly when CRLF is restored. Generated Dataset/refined/mask JSONL remains separately frozen to LF. It also stores exact decoder identity separately, source/model/config provenance availability, selected-candidate derivation, raw-hard/refined-watch output, per-scope validity and machine reasons; complete raw-file byte reconstruction, row counts and absent/miss rows are mandatory.", "id": "ECH2-3"}`
- `{"criterion": "The frozen numeric policy is dimensionally explicit: 70/127 exact lengths; finite coordinates; meters/pixels/camera-root spaces; bbox padding 0.10 of bbox extent; max reprojection error 0.01 px; torso/major-segment minimum 0.02 m and maximum 1.0 m; dimensionless normalization norm >=0.000001 and normalized torso cross magnitude >=0.25881904510252074; camera depth (0.1,100] m; focal (0,100000] px; in-frame tolerance 1 px; per-clip robust major-bone scale deviation <=0.20; adjacent scale jump <=0.10; selected-frame gap <=100 ms; camera-root speed <=12 m/s; max root-relative MHR70 joint speed <=20 m/s; 5-frame [-3,12,17,12,-3]/35 refinement with 0.05 m joint, 0.10 m camera and 2 px safety caps; manual stable-plant confirmation >=100 ms; each hand side and head have >=300 pre-mask observable frames and >=100 post-mask valid frames spanning >=2 clips. Any changed number changes policy/contract/P0 hashes.", "id": "ECH2-4"}`
- `{"criterion": "Mask audit derives every scope boolean, selected subject, warning, global and per-scope reason from Dataset v2 plus frozen manual P0 facts and policy; it never trusts producer-supplied geometry booleans. Torso, head, left/right arm, hand, leg, full-body and contact scopes have independent required joints and reasons: wrist/ankle/head endpoints and palm/full-body/contact metrics use the matching scope, a valid partial-scope row may carry unrelated reasons without becoming globally invalid, and a bad leg/head/opposite limb cannot invalidate an otherwise observable endpoint. Every disabled scored scope has explicit reasons. Contact retains the accepted stronger invariant of inheriting fullBody. Manual raw review/adjudication rows also retain scenario arrays, so a scenario-only disagreement cannot disappear from the compiled audit trail.", "id": "ECH2-5"}`
- `{"criterion": "Standalone closed P0-v2 and P1 external-anchor schemas plus exact audit CLI flags freeze the phase chain. Verified P0/P1 consume concrete anchor paths and independently supplied expected anchor canonical hashes. P0 additionally consumes three role-bound sealed-input paths, while durable sealed descriptors use fixed logicalPath values that are never filesystem-resolved; P1 treats the independently expected P0 anchor as authority and does not require long-term access to those raw inputs. P0 binds the actual r3 authoring schema and compiler/auditor tool bytes in addition to every contract/source/manual artifact. P1 parent is the external P0 anchor canonical hash, not evaluation-pack.json candidate hash. Every inherited P1 descriptor is byte-for-byte/deep-equal to its P0 descriptor and reuses the already verified P0 Buffer snapshot without reopening; externalP0Anchor has a fixed logicalPath while its actual --p0-anchor snapshot is read exactly once. Only P1-new teacher source manifest, Dataset v2, deterministic refined teacher, derived mask, summary and materializer may receive new snapshots. P1 lock binds the complete P1 pack and every source/dataset/refined/mask hash, and a write-once external P1 anchor is mandatory before live improvement. Candidate-only audit is a distinct mode that cannot report frozen/verified.", "id": "ECH2-6"}`
- `{"criterion": "Focused attacks fail with an explicit expected error code through the public audit/CLI path: post-P0 raw-teacher byte substitution, one-frame timestamp substitution, deleted miss row, raw CRLF changed to lone-LF/mixed/lone-CR/missing-terminal or CR incorrectly retained inside rawLineBase64, generated JSONL changed from LF to CRLF, first-person-only dataset, MHR127/bbox/camera loss, detector score as confidence, carry-forward fallback relabeled hit or summary-miss reconciliation drift, empty/missing/invalid detector-bbox fallback, multi-candidate null-anchor plus unusable bbox branch confusion, single-candidate null-anchor plus invalid detector/usable fallback over-derivation, normalized-anchor edge drift, anchor-free multi-candidate selection, bbox/anchor tie, false geometry flags, reprojection drift, first-eight frame-scale/last-two segment separation, torso diagnostic-versus-validity boundary drift, head/hand/leg-only nonfinite temporal scope contamination, missing or nonfinite successfully-selected predecessor, scale/speed threshold drift, unrelated head/arm/hand/leg cross-scope contamination, impossible refined status/null combinations, selection-success non-hit raw-center hash loss, missing endpoint/head/hand-valid scopes or support, contact-without-fullBody, refinement coefficient/window/safety/status/result drift, cross-gap smoothing, raw-hard/refined-watch role switch, scope-reason precedence conflict, scenario-only disagreement erasure, absent-but-observable body state, raw numeric/key-order/null/missing/person-order round-trip drift, candidate-hash-as-parent, substituted arbitrary external anchor, P1 inherited canonical-same/byte-different or auditor-tool full-chain rebind, duplicate/extra/missing/traversal descriptor, sealed role swap/byte-or-inode alias, logicalPath resolver misuse, final-component/ancestor TOCTOU, inside-pack/symlink/hardlink anchor, anchor double-read, P1 dataset/refined/mask/source rehash, compiler-r2 compatibility overclaim, unavailable provenance claimed verified, and any live/student/avatar leakage. Custom and independent Draft 2020-12 validators must agree on the complete valid/invalid artifact matrix. Exact 6,711 source rows, existing tests, npm run check, syntax, diff, scope and temp cleanup pass.", "id": "ECH2-7"}`

Test plan:
- `{"kind": "functional", "maps_to": ["ECH2-1", "ECH2-2", "ECH2-3", "ECH2-4", "ECH2-5", "ECH2-6"], "method": "Build synthetic closed source/manual/teacher-input-inventory/dataset/refined/mask/P0-anchor/P1-pack fixtures, independently recompute detector provenance, selection, projection, scope, temporal guards and exact refinement, verify the P0-frozen raw teacher inventory and full 6,711-row materialization including arms 37 zero-person misses, csi 163 exact carry-forward fallbacks, the arms nonzero first PTS/raw timestamp offset, and require byte/canonical pin agreement."}`
- `{"kind": "edge", "maps_to": ["ECH2-2", "ECH2-3", "ECH2-4", "ECH2-5"], "method": "Exercise zero/one/two candidates including multi+null-anchor+unusable bbox and single+null-anchor+invalid detector/usable fallback, detector-bbox valid versus exact fallback trigger, optional and required anchors, normalized (0,0)/(1,1) to continuous source-pixel coordinates and inclusive expanded-bbox edges, raw person-array order, torso/head/left-right arm/hand/leg/full-body/contact cross-scope isolation, endpoint-versus-palm observability, first successfully-selected frame, post-gap frame, even-count clipScale median, noninteger frame rates, exact 100 ms stable-contact confirmation, absent rows, finite boundary values and all threshold equality cases."}`
- `{"kind": "failure", "maps_to": ["ECH2-1", "ECH2-2", "ECH2-3", "ECH2-4", "ECH2-5", "ECH2-6", "ECH2-7"], "method": "Run every ECH2-7 semantic rehash attack through public runAudit/CLI with an explicit expected error code and prove it fails even after all attacker-controlled self-hashes/descriptors are recomputed. Full-chain materialize inherited canonical-same/byte-different JSON and labelAuditor swaps after P0 verification; require exact P0 snapshot reuse and exactly one P0-anchor open. Exercise arbitrary outside-repository anchors, cwd-relative actual paths, fixed logicalPath versus actual CLI path, sealed A/B/adjudication role/byte/inode aliases, inside-pack sealed inputs, final and ancestor replacement hooks, impossible refined status/pose matrices under both custom and Python Draft validators, selected carry-forward raw-center hashes, torso/scale equality boundaries and scope-local nonfinite predecessors. After a successful P0, delete or move all raw sealed inputs and prove P1 passes without opening them; supplying any sealed flag to P1 must fail argument validation before filesystem access. Include duplicate/extra/missing/traversal descriptors and inside-pack/symlink/hardlink anchors; prove free caller candidate hashes cannot substitute for expected external P0/P1 anchors; prove compiler r2 is deliberately pin-incompatible and real P0 remains blocked until the specified compiler r3 migration."}`
- `{"kind": "regression", "maps_to": ["ECH2-7"], "method": "Run focused contract tests, exact raw-to-decoder count/index audit, all Draft 2020-12 schemas, npm run check, node --check, git diff --check, strict owned/read/forbidden isolation, and zero temporary residue after success/failure/SIGINT/SIGTERM."}`

## Revision History

- `baseline-harness@r1`: discarded (superseded_by=None, reason=None)
- `baseline-harness@r2`: discarded (superseded_by=None, reason=None)
- `baseline-harness@r3`: discarded (superseded_by=None, reason=None)
- `evaluation-contract@r1`: discarded (superseded_by=None, reason=None)
- `evaluation-contract@r2`: discarded (superseded_by=None, reason=None)
- `evaluation-contract-core@r1`: discarded (superseded_by=None, reason=None)
- `manual-p0-freeze@r1`: discarded (superseded_by=None, reason=None)
- `manual-p0-freeze@r2`: discarded (superseded_by=None, reason=None)
- `manual-p0-pack@r1`: discarded (superseded_by=None, reason=None)
- `manual-p0-pack@r2`: discarded (superseded_by=None, reason=None)
- `manual-pack-compiler@r1`: discarded (superseded_by=None, reason=None)
- `manual-review-pass-a@r1`: discarded (superseded_by=None, reason=None)
- `manual-review-pass-a@r2`: discarded (superseded_by=None, reason=None)
- `manual-review-pass-b@r1`: discarded (superseded_by=None, reason=None)
- `manual-review-pass-b@r2`: discarded (superseded_by=None, reason=None)
- `source-pts-manifest@r1`: discarded (superseded_by=None, reason=None)
- `source-pts-manifest@r2`: superseded (superseded_by=r3, reason=Superseded by validated r3 source-only contract/schema design to remove evaluation-contract hash coupling before evaluation hardening.)
- `source-pts-manifest@r3`: discarded (superseded_by=None, reason=None)
- `teacher-contract-hardening@r1`: discarded (superseded_by=None, reason=None)
